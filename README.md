# 大二资料仓库说明

这个仓库当前按学期分支管理资料：

- `sophomore-fall`：大二上资料
- `sophomore-spring`：大二下资料

如果你想查看具体资料，请切换到对应分支。

示例命令：

```bash
git fetch origin
git switch sophomore-fall
```

或：

```bash
git fetch origin
git switch sophomore-spring
```

说明：

- `main` 分支仅保留仓库导航说明，不再存放完整资料文件。
- 仓库中的大文件继续通过 Git LFS 管理。
